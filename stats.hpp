#include "vector3d.hpp"
#include "atom.hpp"

#include <vector> 
#include <math.h> 

float mean(std::vector<float>);
float sum(std::vector<float>);
float variance(std::vector<float>);
float standardDeviation(std::vector<float>);

